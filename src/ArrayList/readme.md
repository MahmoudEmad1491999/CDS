# ArrayList Library

A dynamic array implementation in C for storing dynamically allocated objects by pointer.

## Features
- Dynamic resizing
- Generic data storage (`void*`)
- Custom cleanup functions
- Append/Insert/Remove/Replace elements
- Error handling via callouts

## Installation
```bash
cd arraylist
mkdir build && cd build
cmake .. && cmake --build .